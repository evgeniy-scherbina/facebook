apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: post-search
  labels:
    app: search-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: search-service
