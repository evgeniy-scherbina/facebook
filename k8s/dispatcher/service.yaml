apiVersion: v1
kind: Service
metadata:
  name: dispatcher
  namespace: chat-app
  labels:
    app: dispatcher
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: dispatcher
